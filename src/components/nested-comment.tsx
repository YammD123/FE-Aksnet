import { component$ } from "@builder.io/qwik";

export const NestedComment = component$(({comments, trigerSignal}:{comments:any[], trigerSignal:number}) => {
    
    return(
        <div>
            sds
        </div>
    )
});